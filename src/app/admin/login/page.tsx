"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { LockKeyhole } from "lucide-react";
export default function AdminLoginPage() { const router = useRouter(); const [error,setError]=useState(""); const [pending,setPending]=useState(false); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setPending(true); setError(""); const fd=new FormData(e.currentTarget); const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:fd.get("username"),password:fd.get("password")})}); setPending(false); if(response.ok) { window.dispatchEvent(new Event("hos:login")); router.replace("/admin"); } else setError((await response.json()).error || "Access denied."); } return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="glass-panel w-full max-w-sm rounded-2xl p-7 shadow-[0_0_35px_rgba(139,0,0,.2)]"><LockKeyhole className="mb-5 text-hos-red"/><p className="text-xs font-semibold tracking-[.25em] text-hos-red">HOS CONTROL</p><h1 className="mt-2 text-2xl font-bold text-white">Administrator sign in</h1><p className="mt-2 text-sm text-zinc-400">Secure access for House Of Spanking leadership.</p><label className="mt-7 block text-sm text-zinc-300">Username or email<input name="username" required autoComplete="username" className="mt-2 w-full rounded-lg border border-white/10 bg-black/40 px-3 py-2.5 outline-none focus:border-hos-red"/></label><label className="mt-4 block text-sm text-zinc-300">Password<input name="password" type="password" required autoComplete="current-password" className="mt-2 w-full rounded-lg border border-white/10 bg-black/40 px-3 py-2.5 outline-none focus:border-hos-red"/></label>{error && <p className="mt-4 text-sm text-red-300">{error}</p>}<button disabled={pending} className="mt-6 w-full rounded-lg bg-hos-red px-4 py-3 text-sm font-bold text-white transition hover:bg-red-800 disabled:opacity-60">{pending ? "Authenticating…" : "Sign in"}</button></form></main>; }
